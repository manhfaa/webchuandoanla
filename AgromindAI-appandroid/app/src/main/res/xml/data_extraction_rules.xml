<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing is backed up or transferred.

  The encrypted session blob is useless off-device anyway (its key lives in this
  device's Keystore and cannot be exported), but the Room cache holds leaf photos,
  symptom descriptions and farm locations. Restoring those onto a different phone
  from a cloud backup is not something the grower asked for.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
